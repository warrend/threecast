require "spec_helper"

describe Threecast do
  it "has a version number" do
    expect(Threecast::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
